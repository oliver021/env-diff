{
  "name": "env-drift",
  "version": "1.0.6",
  "description": "Security & Drift Monitor for Secrets and Environment Files",
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "main": "./dist/index.js",
  "bin": {
    "env-drift": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "copy:fixtures": "node -e \"require('fs').cpSync('test/fixtures', 'dist/test/fixtures', {recursive: true, force: true})\"",
    "watch": "tsc -w",
    "test": "npm run build && npm run copy:fixtures && node --test dist/test/*.test.js"
  },
  "keywords": [
    "dotenv",
    "drift",
    "security",
    "audit",
    "cli",
    "monorepo"
  ],
  "author": "Antigravity",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/oliver021/env-diff"
  },
  "homepage": "https://github.com/oliver021/env-diff#readme",
  "bugs": {
    "url": "https://github.com/oliver021/env-diff/issues"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "dotenv": "^16.4.5",
    "js-yaml": "^4.1.0",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  }
}
